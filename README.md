# Env

## How to install

1. Add the package ```philipschlender/env-php``` to the ```require``` or ```require-dev``` element of the ```composer.json``` file

    ```bash
    composer require philipschlender/env-php
    ```
